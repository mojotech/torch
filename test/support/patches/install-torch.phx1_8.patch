diff --git a/config/config.exs b/config/config.exs
index 76736cc..958e01f 100644
--- a/config/config.exs
+++ b/config/config.exs
@@ -60,6 +60,8 @@ config :logger, :default_formatter,
 # Use Jason for JSON parsing in Phoenix
 config :phoenix, :json_library, Jason

+config :torch, otp_app: :phx1_8
+
 # Import environment specific config. This must remain at the bottom
 # of this file so it overrides the configuration defined above.
 import_config "#{config_env()}.exs"
diff --git a/lib/phx1_8_web/endpoint.ex b/lib/phx1_8_web/endpoint.ex
index bc7918a..6394cc0 100644
--- a/lib/phx1_8_web/endpoint.ex
+++ b/lib/phx1_8_web/endpoint.ex
@@ -26,6 +26,12 @@ defmodule Phx18Web.Endpoint do
     gzip: not code_reloading?,
     only: Phx18Web.static_paths()

+  plug Plug.Static,
+    at: "/torch",
+    from: {:torch, "priv/static"},
+    gzip: true,
+    cache_control_for_etags: "public, max-age=86400"
+
   # Code reloading can be explicitly enabled under the
   # :code_reloader configuration of your endpoint.
   if code_reloading? do
diff --git a/mix.exs b/mix.exs
index e24d7ed..bf5c265 100644
--- a/mix.exs
+++ b/mix.exs
@@ -65,7 +65,9 @@ defmodule Phx18.MixProject do
       {:gettext, "~> 0.26"},
       {:jason, "~> 1.2"},
       {:dns_cluster, "~> 0.2.0"},
-      {:bandit, "~> 1.5"}
+      {:bandit, "~> 1.5"},
+      {:ssl_verify_fun, manager: :rebar3, runtime: false, override: true},
+      {:torch, path: "../../../../"}
     ]
   end


#!/usr/bin/env bash

mix local.hex --force;
mix local.rebar --force;
mix deps.get;
mix compile;

npm install;

cd apps/example && {
  mix deps.get;
  MIX_ENV=test mix ecto.create;
  mix test
  cd -;
}

mix credo --strict
